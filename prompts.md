## Prompt 1:
I want to build the following game:

Pico Firme Game

It has the following rules:
    - The computer chooses a random 4-digit number.
    - Digits cannot repeat.
    - The player guesses the number.
    - Firme means the correct digit is in the correct place.
    - Pico means the correct digit is present but in the wrong place.
    - The game continues until the player guesses the correct number.

First, suggest me three different algorithms to generate the secret 4-digit number with no repeated digits. Remember that this is a react-typescript vite app.

Write up your reasoning on these three algorithms under the `plans` folder in a `.md` file.

**Output results are in `plans/secret-number-algorithms.md` file.**

---

## Prompt 2:
Using our React-Typescript Vite framework, make me a plan on how to implement this Pico Firme Game. Remember, these are the rules:
    - The computer chooses a random 4-digit number.
    - Digits cannot repeat.
    - The player guesses the number.
    - Firme means the correct digit is in the correct place.
    - Pico means the correct digit is present but in the wrong place.
    - The game continues until the player guesses the correct number.

Let's use the Fisher-Yates Shuffle algorithm for generating the 4-digit number. Write up the implementation plan under `plans` in a `.md` file.

**Output results are in `plans/implementation-plan.md` file.**

---

## Prompt 3:
Use the frontend-design skill to enhance the overall aesthetics of this game. Plan out your changes under the `plans` folder in a `.md` file.

**Output results are in `plans/aesthetic-enhancement.md` file.**

---

## Prompt 4:
Use the frontend-design skill to address the following:
    - Avoid the superfluous use of purple
    - The input placeholder text `Enter 4 digits` does not fit
    - Make any other improvements that the skill deems necessary

**Output results are in `screenshots/Prompt_4_Output.png` file.**

---

## Prompt 5:
Use the frontend-design skill to have the guesses in its own scrollable element rather than just endlessly extending the app page.

**Output results are in `screenshots/Prompt_5_Output.png` file.**

---

## Prompt 6:
Now enhance the overall app layout so it renders beautifully for both lage desktop screens as well as mobile screens. Ideally, it should adjust for any size screen.

**Output results are in `screenshots/Prompt_6_Output.png` file.**

---

## Prompt 7:
Use git to initialize this project. Proceed with committing all files.